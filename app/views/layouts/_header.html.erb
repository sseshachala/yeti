<header>
<nav class="round">
 <ul>
   <% if signed_in? %>
   <li><%= link_to "Profile", show_path("users",  @current_user.attributes["username"]) %></li>
   <% end %>
  <% if signed_in? %>
   <li><%= link_to "Sign out", signout_path, :method => :delete %></li>
  <% else %>
   <li><%= link_to "Sign in", signin_path %>  </li>
 <% end %>
  <% if signed_in? %>
   <%if current_user.admin? %>
    <li><%= link_to "Manage Users", users_path %></li>
    <li><%= link_to "Manage Monitors", rmonitors_path %></li>
   <% end %>
  <% end %>
 <ul>
</nav>
</header>
